name: ares_asistan
description: "Ares Asistan Uygulamasi"
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1
  speech_to_text: ^6.6.2
  flutter_tts: ^4.0.2
  audioplayers: ^6.0.0
  image_picker: ^1.1.2
  file_picker: ^8.0.0
  http: ^1.2.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
